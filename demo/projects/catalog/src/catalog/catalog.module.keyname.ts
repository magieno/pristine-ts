/**
 * Keyname for `CatalogModule`. Used by every `@moduleScoped(CatalogModuleKeyname)` service
 * inside the catalog feature (`ProductService`, `ProductsController`, `SyncCatalogCommand`).
 */
export const CatalogModuleKeyname = "catalog";
