describe("HTTP Client", () => {
  it("should properly pass back the intercepted request", () => {
    expect(true).toBeTruthy();
  })
  it("should properly pass back the intercepted response", () => {
    expect(true).toBeTruthy();
  })
})
