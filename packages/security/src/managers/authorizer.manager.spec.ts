describe("AuthorizerManager", () => {
    it("should authorize if there are no guards defined in the context or if the route context is not properly defined", () => {

    })

    it("should call every single guards defined in the RouteContext", () => {

    })

    it("should deny the authorization even if only one of many guards denies access", () => {

    })

    it("should deny if one guard throws an exception", () => {

    })

    it("should call the setContext method before calling the 'isAuthorized' method", () => {

    })
})
