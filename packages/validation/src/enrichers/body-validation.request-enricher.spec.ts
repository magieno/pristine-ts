describe("Body Validation Request Enricher", () => {
    it("should simply return the request if the bodyValidator is undefined", () => {
        
    })

    it("should return the request if there are no errors with the instance", () => {

    })

    it("should reject if there are validation errors. ", () => {

    })
})
