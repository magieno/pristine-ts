describe("JWT Module instantiation in the Kernel", () => {
    it("should instantiate the JWT Module", () => {

    })

    it("should throw an error when the configuration is not specified", () => {

    })
})